{
  "name": "ai-humanaizer",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build && npm run copy:prompts",
    "copy:prompts": "rm -rf packages/en-humanizer/dist/prompts packages/uk-humanizer/dist/prompts && cp -r prompts/en packages/en-humanizer/dist/prompts && cp -r prompts/uk packages/uk-humanizer/dist/prompts",
    "build:shared": "tsc --build packages/shared",
    "build:en": "tsc --build packages/en-humanizer",
    "build:uk": "tsc --build packages/uk-humanizer"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3"
  }
}
